#!/bin/sh
#
# Fichero de exclusion de proyectos para herramienta de Autoversionado
#
# Uso: Se utiliza la variable exclusionArray para enumerar los proyectos que a los que no se desee aplicar el autoversionado.
#

declare -a exclusionArray=("proyect1" "project2" "proyecto3")